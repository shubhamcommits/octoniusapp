# To decrypt the folder you first need to install git-crypt

- osx
`brew install git-crypt`

- windows 
`pacman -S git-crypt`

- linux 
`apt-get install git-crypt`

### After install runn the command to unloac

`git-crypt unloack /path/to/key`