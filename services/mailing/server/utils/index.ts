import { sendErr } from './sendError';
import * as sendMail from './sendmail/sendmail';

/*  =====================
 *  -- UTILS EXPORTS --
 *  =====================
 * */
export {
    sendErr as sendError,
    sendMail as sendMail
}