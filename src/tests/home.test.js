const puppeteer = require('puppeteer');

let browser;

test('Launch a browser', async () => {
  //  // Define maximum time to await for the test to finish
  //  jest.setTimeout(15000);
  // 
  //  browser = await puppeteer.launch({ headless: false });
  //  const page = await browser.newPage();
  // 
  //  await page.goto('localhost:3000');
  // 
  //  const btnJoin = await page.$eval('button.btn.btn-primary', el => el.innerHTML);
  // 
  //  expect(btnJoin).toEqual('Join your workplace');
  // 
  //  await browser.close();
});
